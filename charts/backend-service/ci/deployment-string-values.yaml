# CI test: string CPU values + custom ports (merged from ports-values.yaml)
image:
  repository: "ghcr.io/unique-ag/chart-testing-service"
  tag: "1.1.0"
resources:
  limits:
    cpu: 100m
    memory: 1Gi
  requests:
    cpu: 100m
    memory: 128Mi
ports:
  application: 8080
  service: 9001
service:
  port: null
