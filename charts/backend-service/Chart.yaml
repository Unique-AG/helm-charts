apiVersion: v2
type: application
icon: https://www.unique.ch/hubfs/unique_assets/favicon/glyph_caumasee.svg
maintainers:
  - name: unique-ag
    url: https://unique.ch/
name: backend-service
sources:
  - https://github.com/Unique-AG/helm-charts/tree/main/charts/backend-service
# changes to consider on next major: enable netpol by default, deprecate cronJob, remove external-secrets
version: 6.0.0
appVersion: latest
description: |
  The 'backend-service' chart is a "convenience" chart from Unique AG that can generically be used to deploy backend workloads to Kubernetes.

  Note that this chart assumes that you have a valid contract with Unique AG and thus access to the required Docker images.
annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/changes: |
    - kind: changed
      description: "⚠️ KEDA scalers from list to map structure and renamed to triggers for better overlay support, see upgrade guide for more details."
    - kind: changed
      description: "⚠️ external-secrets.io/* to external-secrets.io/v1, see upgrade guide for more details."
