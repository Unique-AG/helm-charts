apiVersion: v2
type: application
icon: https://www.unique.ch/hubfs/unique_assets/favicon/glyph_caumasee.svg
maintainers:
  - name: unique-ag
    url: https://unique.ch/
name: backend-service
sources:
  - https://github.com/Unique-AG/helm-charts/tree/main/charts/backend-service
# changes to consider on next major: enable netpol by default, deprecate cronJob
version: 9.0.0
appVersion: latest
description: |
  The 'backend-service' chart is a "convenience" chart from Unique AG that can generically be used to deploy backend workloads to Kubernetes.

  Note that this chart assumes that you have a valid contract with Unique AG and thus access to the required Docker images.
annotations:
  artifacthub.io/license: Apache-2.0
  artifacthub.io/changes: |
    - kind: changed
      description: "⚠️ BREAKING: Replaced auditVolume with auditVolumes - cloud-agnostic, blue-green migration-ready structure."
    - kind: added
      description: "Added AUDIT_LOG_DIR environment variable, automatically set based on auditVolumes.target."
    - kind: added
      description: "Cloud provider auto-detected by presence of azure field (aws/gcp planned)."
    - kind: added
      description: "Added explicit name field for backward-compatible PV/PVC naming."
    - kind: added
      description: "Added support for direct paths (e.g., /dev/stdout) as audit target."
