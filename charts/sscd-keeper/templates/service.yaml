apiVersion: v1
kind: Service
metadata:
  name: {{ include "sscd-keeper.fullname" . }}
  labels:
    {{- include "sscd-keeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sscd-keeper.selectorLabels" . | nindent 4 }}
