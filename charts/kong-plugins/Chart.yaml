apiVersion: v2
type: application
icon: https://www.unique.ch/hubfs/unique_assets/favicon/glyph_caumasee.svg
maintainers:
  - name: unique-ag
    url: https://unique.ch/
name: kong-plugins
version: 2.1.0
description: |
  The 'kong-plugins' chart provides a streamlined solution for deploying Kong plugins via ConfigMaps to be used with the Unique Software.

  Refer to each plugins readme section to learn more about them.

  Please report any security concerns with the plugins via the [Security Policy](https://github.com/Unique-AG/helm-charts/tree/main?tab=security-ov-file).
annotations:
  artifacthub.io/changes: |
    - kind: added
      description: Configurable ssl_verify option for cluster-internal setups without TLS
    - kind: changed
      description: Tune log levels - reduce per-request INFO noise, promote key rotation events to INFO
    - kind: security
      description: Add WARN logs with client IP for suspicious auth rejections (invalid issuer, algorithm, signature, malformed token)
